{
    "require-dev": {
        "psalm/plugin-phpunit": "^0.18.3",
        "vimeo/psalm": "^4.10"
    },
    "config": {
        "preferred-install": {
            "*": "dist"
        },
        "sort-packages": true
    }
}
